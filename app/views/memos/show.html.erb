<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @memo.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @memo.content %>
</p>




<%= form_for [@memo, Reply.new] do |f| %>
	<%= f.text_area :content %>
	<%= f.submit %>
<% end %>

<%= link_to 'Edit', edit_memo_path(@memo) %> |
<%= link_to 'Back', memos_path %>
